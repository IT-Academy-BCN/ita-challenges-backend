spring:
  application:
    name: itachallenge-auth
    version: 2.0.0-RELEASE
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  profiles:
    active: local
  jmx:
    enabled: true
  security:
    oauth2:
      client:
        registration:
          github:
            environments:
              local:
                client-id: ${GITHUB_CLIENT_ID}
                client-secret: ${GITHUB_CLIENT_SECRET}
                redirect-uri: http://localhost:4200/ita-challenge/challenges
              dev:
                client-id: ${GITHUB_CLIENT_ID}
                client-secret: ${GITHUB_CLIENT_SECRET}
                # redirect-uri: http://localhost:4200/ita-challenge/challenges
                redirect-uri: https://dev.ita-challenges.eurecatacademy.org/ita-challenge/challenges #redirect_uri real de dev
        provider:
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user
            user-name-attribute: login

springdoc:
  swagger-ui:
    path: "/swagger-ui-custom.html"
    operationsSorted: method
  api-docs:
    enable: true
    path: "/api-docs"

server:
  port: 8761

management:
  endpoints:
    web:
      exposure:
        include: "*"
  security:
    enabled: false

uri_validate_token : https://dev.sso.itawiki.eurecatacademy.org/api/v1/tokens/validate

user:
  service:
    url: http://apisix-gateway:9080 # IN PRODUCTION
    #url: http://172.17.0.2:8764 # LOCAL IP

logging:
  level:
    root: INFO
    "[org.springframework]": ERROR
    "[com.businessassistantbcn]": INFO
  pattern:
    # console: Spring's default
    file: "%date %5level %-40.40logger{39} [%thread] %msg%n"
#  file:
#    name: itachallenge-challenge.log

token:
  signing:
    key: someRandomStringToProtectThisAppFromAttacks
  expiration:
    minutes: 600
