#ReactiveMongoDB configuration
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: itac-users

#logging
logging:
  level:
    org.springframework.data.mongodb: DEBUG

