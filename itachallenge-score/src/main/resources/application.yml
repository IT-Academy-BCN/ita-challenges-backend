spring:
  application:
    name: itachallenge-score
  jmx:
    enabled: true
    
  #profiles: dev: no security; prod: security activated
  profiles:
    active:
    - prod
    
  security:
    datasource:
      secret: SgVkYp3s6v9yBEH+MbQeThWmZq4t7p09ventepamadrid
      headerString: Authorization
      authoritiesClaim: authorities
      err: Auth Error, Token Unavailable 
      tokenExpiration: 3600000
      

springdoc:
  swagger-ui:
    path: "/swagger-ui-custom.html"
    operationsSorted: method
  api-docs:
    enable: true
    path: "/api-docs"

server:
  port: 8763

management:
  endpoints:
    web:
      exposure:
        include: "*"
  security:
    enabled: false




logging:
  level:
    root: INFO
    "[org.springframework]": ERROR
    "[com.businessassistantbcn]": INFO
  pattern:
  # console: Spring's default
    file: "%date %5level %-40.40logger{39} [%thread] %msg%n"
#  file:
#    name: itachallenge-score.log

