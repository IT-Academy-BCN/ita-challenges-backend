
apisix:
  enable_admin: false
  config_center: yaml

routes:
  - uri: /*
    upstream:
      nodes:
        "172.18.0.1:8762": 1
      type: roundrobin

deployment:
  role: data_plane
  role_data_plane:
    config_provider: yaml
#END
