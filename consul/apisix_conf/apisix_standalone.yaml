
apisix:
  enable_admin: false
  config_center: yaml

routes:
  - uri: /test
    upstream:
      nodes:
        "127.0.0.1:7779": 1
      type: roundrobin

deployment:
  role: data_plane
  role_data_plane:
    config_provider: yaml


#END
