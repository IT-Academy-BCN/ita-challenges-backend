
apisix:
  enable_admin: false
  config_center: yaml

routes:
  - uri: /test
    upstream:
      nodes:
        "172.18.0.9:7779": 1
      type: roundrobin

deployment:
  role: data_plane
  role_data_plane:
    config_provider: yaml
#END
